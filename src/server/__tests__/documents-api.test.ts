import { describe, it, expect } from 'vitest';
// import request from 'supertest';
// import app from '../../app';

describe('/api/documents', () => {
  it('should create, fetch, and delete a document (stub)', async () => {
    // const res = await request(app).post('/api/documents').send({ title: 'Test', content: '...' });
    // expect(res.status).toBe(201);
    // const docId = res.body.id;
    // const getRes = await request(app).get(`/api/documents/${docId}`);
    // expect(getRes.status).toBe(200);
    // await request(app).delete(`/api/documents/${docId}`);
    expect(true).toBe(true); // Placeholder
  });
});
